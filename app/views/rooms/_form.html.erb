<%= form_for(room) do |f| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %>
    <%= f.text_field :number %>
  </div>

  <div class="field">
    <%= f.label :size %>
    <%= f.select :size, ['Small', 'Medium' ,'Large']%>
  </div>

  <div class="field">
    <%= f.label :location %>
    <%= f.select :location, ['James.B.Hunt', 'D.H.Hill']%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
